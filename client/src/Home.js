import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
    return (
        <div>
            <Typography variant="h4">Bienvenido a la Aplicación de Notificaciones</Typography>
            <Typography variant="body1">Navega a /notifications para ver las notificaciones.</Typography>
        </div>
    );
};

export default Home;
